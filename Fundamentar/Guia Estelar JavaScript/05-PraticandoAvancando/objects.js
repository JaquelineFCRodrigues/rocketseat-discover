// Objects ----------------------------------------------------------------------

const person = {
    name: 'John',
    age: 34,
    weight: 100,
    isAdmin: true,
};

console.log(`${person.name} tem ${person.age} anos`);

